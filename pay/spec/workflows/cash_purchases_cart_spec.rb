require "rails_helper"

describe CashPurchaseCart, :aggregate_failures do
  
  describe "successful cash purchase" do
    let() {}
    let() {}
    let() {}

    let() {}
    let() {}
    let() {}
    let() {}
    let() {}
    let() {}
    let() {}
    let() {}
    let() {}

    context "" do
      before() do
        allow().to receive().and_return()
	[].each {}
      end

      it "" do
        workflow.run
	expect().to be_purchased
	expect().to be_purchased
	expect().not_to be_purchased
	expect().to be_truthy
	expect().to match()
	expect().to be_truthy
	expect().to eq()
      end
    end

    context "" do
    
      before() do
        allow().to receive().and_return()
      end

      it "" do
        expect {}.to raise_error()
	expect().to be_falsy
	expect().to be_nil
	expect().not_to be_purchased
	expect().not_to be_purchased
	expect().not_to be_purchased
      end

    end

  end

end


